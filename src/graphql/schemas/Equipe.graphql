type Equipe {
    id: Int!
    nome: String!
    participantes: [Participante!]!
    categoria: Categoria
}

input EquipeInput {
    participantes: [ParticipanteInput]
}

type PontuacaoEquipe {
    nome: String!
    pontuacao: Int!
}